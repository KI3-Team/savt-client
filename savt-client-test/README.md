## Python gRPC Client Quick Test

1. First, generate the Python gRPC files from the proto definition:

```bash
bash generate.sh
```

2. Then, run the test script to verify the gRPC interface:

```bash
python test_savt_ipc.py
```